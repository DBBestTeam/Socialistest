<div class="well">
  <%= form_for(@post) do |f| %>
    <div class="form-group">
      <h4>Actualizar estado</h4>
      <%= f.text_area :content, class: 'form-control', autofocus: true, placeholder: '¿Qué estás en tu mente, nene?' %>
    </div>
    <div id="img-preview" class="form-group hidden">
      <img src="" alt="Preview">
    </div>
    <div class="form-group hidden">
      <%= f.file_field :attachment %>
    </div>
    <div class="form-group">
      <a id="post-attachment" href="javascript:void(0);" class="pull-right btn btn-primary">
        <%= fa_icon 'image' %>
      </a>
      <%= f.submit 'Post', class: 'btn btn-primary'%>
    </div>
  <% end %>
</div>